{
  "scripts": {
    "deploy": "serverless deploy --force",
    "remove": "serverless remove"
  },
  "devDependencies": {
    "serverless": "^2.60.0",
    "serverless-plugin-architecture": "https://github.com/qubops/serverless-plugin-architecture"
  },
  "engines": {
    "node": ">=18"
  }
}
